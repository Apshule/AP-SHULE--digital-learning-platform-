Task 5 — Step 3 of 3: AI activity generator polish, performance, final testing

CONTEXT:
- Step 1 built curriculum_links collection, CSV/JSON import, seed data, offline cache
- Step 2 built search modal, 3-tab detail view, favorites, offline search
- Now we polish the AI generator, optimize performance, and run final tests

GOAL:
Make the Curriculum Linker production-ready:
- Robust AI activity generation (retry, error handling, PDF export)
- Fast search even with 1000+ topics
- Cross-device consistency
- Full regression testing with Tasks 1-4

═══════════════════════════════════════════════════════════════
PART A: AI ACTIVITY GENERATOR — ROBUST IMPLEMENTATION
═══════════════════════════════════════════════════════════════

Improve the Gemini API call from Step 2:

1. Add retry logic:
   - If API fails, retry up to 2 more times with 2-second delay
   - Show "Retrying (1/2)..." message during retries
   - After 3 total failures, fall back to cached activitySuggestion

2. Add timeout:
   - Abort API call if no response within 20 seconds
   - Use AbortController
   - Show timeout message: "AI took too long. Showing saved activity."

3. Cache successful generations:
   - Store each generated activity in a local cache keyed by 
     topic + subject + classLevel
   - Add new Firestore collection ai_activity_cache:
     { topic, subject, classLevel, activityText, generatedAt, 
       generatedBy, hits }
   - Before calling Gemini, check cache first (if entry < 30 days old)
   - This saves API quota and speeds up repeated searches

4. Increment hit counter when cached activity is reused

═══════════════════════════════════════════════════════════════
PART B: AI ACTIVITY — PDF EXPORT
═══════════════════════════════════════════════════════════════

Add a proper PDF export for generated activities:

Use jsPDF (already loaded in Task 3 Step 7).

generateActivityPdf(activityData):
- Header: "CBC Activity — {topic}" + AppShule logo text
- Sub-header: Subject | Class | Generated on date
- Body: activity text (auto-formatted with word wrap)
- Footer: "Generated by AppShule AI Tutor — NCDC-aligned"
- Filename: CBC_Activity_{topic}_{date}.pdf

Test the PDF opens with correct formatting.

═══════════════════════════════════════════════════════════════
PART C: SEARCH PERFORMANCE OPTIMIZATION
═══════════════════════════════════════════════════════════════

The search must be fast even with 1000+ topics.

1. Add client-side caching:
   - On first search, fetch all curriculum_links (limit 2000)
   - Cache in memory for the session
   - Subsequent searches filter the cache locally
   - Only re-fetch if: cache is empty OR user clicks [Refresh]

2. Debounce improvement:
   - Current: 300ms debounce
   - Add: cancel previous search if new input arrives
   - Use AbortController or search token

3. Add search suggestions dropdown:
   - As user types, show top 5 topic suggestions below input
   - Tap a suggestion → runs full search instantly

4. Results rendering:
   - Use document fragment for batch DOM inserts
   - Limit to 20 results
   - Add "Load more" button if there are more matches

═══════════════════════════════════════════════════════════════
PART D: CROSS-DEVICE SYNC FOR FAVORITES
═══════════════════════════════════════════════════════════════

Verify and refine favorites sync (started in Step 2):

1. When teacher logs in on new device:
   - Fetch teacher_favorites for their uid
   - Merge with local IndexedDB favorites
   - Write merged set back to Firestore

2. When favoriting offline:
   - Save to IndexedDB offline_favorites
   - On reconnect: push to Firestore
   - Use arrayUnion to avoid duplicates

3. Conflict handling:
   - If same favorite added on two devices: no duplicate (docId is key)

4. Add "Sync Now" button inside My Favorites tab
   - Shows pending count if offline

═══════════════════════════════════════════════════════════════
PART E: POPULAR TOPICS SUGGESTIONS
═══════════════════════════════════════════════════════════════

Improve the "Popular topics" section (Step 2 Part B.3):

1. Track search frequency:
   - Add search_count field to curriculum_links docs
   - Increment via Firestore increment() when a topic is tapped

2. Show popular topics:
   - Query curriculum_links ORDER BY search_count DESC LIMIT 8
   - Cache result for 5 minutes

3. If no search data yet:
   - Show 8 random topics as fallback

4. Add a "Recently viewed" section below Popular:
   - Store last 5 viewed topic IDs in IndexedDB
   - Show them as quick-access chips

═══════════════════════════════════════════════════════════════
PART F: SUGGEST-A-TOPIC FORM
═══════════════════════════════════════════════════════════════

When teacher searches and gets no results, they can suggest a topic:

1. Click [Suggest a topic] button
2. Modal opens with:
   - Topic name (text input, required)
   - Subject (dropdown, required)
   - Class level (dropdown, required)
   - Why is this needed? (textarea, optional)
   - [Submit Suggestion] button
3. Writes to Firestore: topic_requests collection
   { topic, subject, classLevel, reason, 
     requestedBy: uid, requestedByName, createdAt }
4. Show toast: "Suggestion submitted — admin will review."

Super Admin side:
- Add a section in Admin Panel: "📝 Topic Requests"
- List all pending requests
- Each has [Approve] [Reject] buttons
- Approve → creates a placeholder curriculum_links doc 
  (admin must fill in details)

═══════════════════════════════════════════════════════════════
PART G: ACCESSIBILITY AND UX POLISH
═══════════════════════════════════════════════════════════════

1. Keyboard navigation:
   - Arrow keys navigate result cards
   - Enter opens detail view
   - Escape closes modals

2. Screen reader support:
   - Add aria-labels to buttons
   - Add role="search" to search input
   - Announce result counts via aria-live

3. Loading states:
   - Skeleton cards while search runs
   - Spinner for AI generation
   - Empty state icons

4. Toast feedback:
   - "Copied to clipboard" when Copy button clicked
   - "Saved to favorites" / "Removed from favorites"
   - "PDF downloaded"

═══════════════════════════════════════════════════════════════
PART H: MOBILE RESPONSIVENESS
═══════════════════════════════════════════════════════════════

Ensure the Curriculum Linker works on all devices:

1. Phone (< 500px):
   - Full-screen modal
   - Single-column results
   - Tabs stack vertically if needed
   - Touch-friendly tap targets (44px min)

2. Tablet (500px - 900px):
   - Modal centered, max 600px wide
   - Two-column result cards

3. Desktop (> 900px):
   - Side panel opens from right (max 500px)
   - Results list scrollable
   - Detail view replaces results

Test at 3 breakpoints to confirm layout.

═══════════════════════════════════════════════════════════════
PART I: FIRESTORE INDEXES
═══════════════════════════════════════════════════════════════

Ensure these composite indexes exist:

1. curriculum_links:
   - subject (asc) + classLevel (asc) + topic (asc)
   - search_count (desc)

2. teacher_favorites:
   - teacherId (asc) + savedAt (desc)

3. topic_requests:
   - status (asc) + createdAt (desc)

4. ai_activity_cache:
   - topic (asc) + subject (asc) + classLevel (asc)

Replit should print index requirements to console if missing.

═══════════════════════════════════════════════════════════════
PART J: FULL REGRESSION TESTING
═══════════════════════════════════════════════════════════════

Run this comprehensive regression suite:

TEST 1 — Task 1 features intact:
   ☐ NCDC/UNEB buttons appear
   ☐ Teacher CBC Teaching Tools visible
   ☐ Auto Lesson Plan Generator works
   ☐ Triangulation Assessment works

TEST 2 — Task 2 features intact:
   ☐ Offline badge shows
   ☐ Offline login works
   ☐ Sync button functional
   ☐ Connection mode detection works

TEST 3 — Task 3 features intact:
   ☐ Teacher Offline Earnings Summary shows
   ☐ PDF earnings report downloads
   ☐ Super Admin Offline Views Overview works

TEST 4 — Task 4 features intact:
   ☐ Student My Projects works
   ☐ QR code generation works
   ☐ Teacher verification + viva works
   ☐ Evidence Folder shows
   ☐ Duplicate detection works

TEST 5 — Task 5 features:
   ☐ Search works online
   ☐ Search works offline
   ☐ Filters work
   ☐ 3 tabs show correct data
   ☐ AI activity generation works
   ☐ PDF export of activity works
   ☐ Favorites save + sync
   ☐ Popular topics appear
   ☐ Suggest-a-topic submits
   ☐ Cross-device favorites sync

═══════════════════════════════════════════════════════════════
PART K: PERFORMANCE BENCHMARKS
═══════════════════════════════════════════════════════════════

Measure and confirm:

1. First search (cold cache): <2 seconds
2. Subsequent searches (warm cache): <300ms
3. AI activity generation: <10 seconds
4. PDF export: <2 seconds
5. Modal open: <500ms
6. Offline search: <500ms

If any benchmark fails, optimize:
- Add indexes
- Cache aggressively
- Lazy-load images
- Compress Firestore responses

═══════════════════════════════════════════════════════════════
PART L: CODE CLEANUP
═══════════════════════════════════════════════════════════════

After all tests pass:
1. Remove console.log debug statements
2. Verify no syntax errors (check DevTools Console)
3. Check for unused imports or dead code
4. Add clear code comments for:
   - Search flow
   - Cache strategy
   - Offline fallback logic
   - AI generation retry logic

═══════════════════════════════════════════════════════════════
PART M: FINAL REPORT
═══════════════════════════════════════════════════════════════

Report back with:

1. Feature completeness:
   - Curriculum Linker: search, filters, tabs, AI, favorites, 
     offline, suggest, cross-device sync
   - All present? Yes/No

2. Regression results:
   - Task 1: all pass?
   - Task 2: all pass?
   - Task 3: all pass?
   - Task 4: all pass?
   - Task 5: all pass?

3. Performance:
   - Benchmarks met?
   - Any slow operations?

4. Recommendations for Task 6:
   - Prerequisites needed
   - Technical debt to note

If everything passes:
"Task 5 fully complete — Curriculum Linker with search, 
3-tab results, AI activity generation, favorites, offline mode, 
and cross-device sync all working. Ready for Task 6."

═══════════════════════════════════════════════════════════════
DO NOT
═══════════════════════════════════════════════════════════════

- Do NOT rebuild the app
- Do NOT break Tasks 1, 2, 3, 4 or Steps 1-2 of Task 5
- Do NOT fetch NCDC content from external URLs
- Do NOT exceed 2000 cached curriculum records
- Do NOT block UI during search
- Do NOT lose favorites when offline
- Do NOT exceed Gemini API free tier (cache aggressively)

═══════════════════════════════════════════════════════════════

Build Parts A-L, then run the regression tests in Part J.

Confirm when done with:
"Task 5 Step 3 complete — Curriculum Linker fully polished and tested. 
Ready for Task 6."

Report:
- Which regression tests passed
- Performance benchmark results
- Any remaining issues
- Screenshots recommended: AI activity PDF, popular topics, 
  suggest-topic modal, mobile view, tablet view, desktop view